# TASKS.md

## 実装方針

実装は垂直スライス方式で行う。各フェーズでDB、RLS、API、UI、テスト、文書を完成させる。

各フェーズ終了時：

1. `pnpm verify:commit` を成功させる
2. コミットする
3. 完了報告する
4. 停止する
5. ユーザーの続行指示を待つ

---

# Phase 1: 基盤・認証

## 目的

Next.js、Supabase、Google OAuth、テスト、デザイン基盤を構築する。

## 実装

- Next.js App Router + TypeScript + pnpm
- ESLint、Prettier
- Vitest、React Testing Library、Playwright
- Husky等によるGit hook
- `pnpm verify:commit`
- GitHub Actions
- Supabase CLI初期化
- Google OAuth
- ログイン、ログアウト、認証コールバック
- 認証済みレイアウト
- 未認証ガード
- 基本ヘッダー、左ナビの枠
- デザイントークン
- ライトモード
- `noindex`、robots、X-Robots-Tagの基礎
- ローカルE2E用のテスト認証手段

## テスト

- 未認証アクセス
- ログイン導線
- 認証済みレイアウト
- ログアウト
- noindex関連
- CIとGit hook

## 完了条件

- ローカルSupabaseで認証済み状態を再現可能
- localhostのGoogle OAuthがlocalhostへ戻る
- 本番用Google OAuth設定手順をREADMEへ記載
- 全検証成功

---

# Phase 2: プロジェクト管理

進捗：実装完了（Phase 3の開始待ち）

## 目的

プロジェクトの作成、一覧、更新、完全削除を実装する。

## DB

- `projects`
- `project_settings`
- 所有者RLS
- `ON DELETE CASCADE`
- 公開状態は初期値private

## UI

- プロジェクト一覧
- 新規プロジェクト
- 作成成功後にタイムラインを表示
- 最小入力＋詳細設定
- テンプレート選択
- プロジェクト設定
- 完全削除
- 左ナビへプロジェクト一覧
- 一覧カードからタイムラインへ遷移
- 説明未入力カードの高さ維持と代替文非表示
- モバイル左ナビの遷移時自動閉鎖

## テスト

- 所有者CRUD
- 他ユーザー拒否
- 名前必須
- 非公開初期値
- 削除確認
- カスケード削除
- E2E：ログイン後に作成・編集・削除

## 完了条件

- プロジェクト単位の基礎権限が成立
- 全検証成功

---

# Phase 3: 対象種別管理

進捗：実装完了（Phase 4の開始待ち）

## 目的

プロジェクトごとに編集可能な対象種別マスタを実装する。

## DB

- `timeline_item_types`
- 正規化名による重複防止
- 色、アイコン、並び順、表示状態
- テンプレート別初期データ

## UI

- 一覧
- 追加
- 名称変更
- 色変更
- 並べ替え
- 非表示
- 未使用種別の削除
- 検索・新規作成対応コンボボックス

## テスト

- 重複名
- 並べ替え
- 色検証
- 所有者権限
- 使用中削除の準備となる制約
- E2E：追加・変更・並べ替え

## 完了条件

- 文学史、美術史、思想史、汎用、空テンプレートが作成時に反映
- 全検証成功

---

# Phase 4: タイムラインアイテム

進捗：実装完了（Phase 5の開始待ち）

## 目的

期間型・時点型項目の登録と基本描画を実装する。

## DB

- `timeline_items`
- temporal_type
- start／end／point
- end_date_status
- last_confirmed
- 曖昧フラグ
- 個別色
- manual_order
- source_text
- DB制約

## UI

- サイドパネル簡易登録
- 詳細情報の折りたたみ編集
- 期間／時点切替
- 終了状態
- 曖昧フラグ
- 種別色継承と個別上書き
- 基本横棒・時点型マーカー
- 手動順
- 自動並べ替え
- 対象種別の `sort_order` による並べ替え
- グループ表示
- 折りたたみ
- 非表示項目の最下部専用グループ
- グループ見出しを除外した表示行数
- 完全削除
- タイムライン内の設定・対象種別右パネル
- 対象種別編集後の登録フォーム選択肢同期

## テスト

- 日付検証
- 終了状態別制約
- 曖昧開始・終了
- 継続中
- 終了不明・最終確認日
- 時点型
- 所有者RLS
- D&D
- E2E：項目登録と描画

## 完了条件

- 期間型と時点型が正しく保存・表示
- 全検証成功

---

# Phase 5: 時間軸操作

進捗：実装完了（Phase 6の開始待ち）

## 目的

パン、ズーム、自動目盛り、仮想化を実装する。

## 実装

- 歴史日付型
- 日付⇔X座標
- 段階式ズーム
- カーソル中心ズーム
- `Alt + ホイール`ズーム
- ボタン／スライダー
- 横ドラッグ
- パン中のスクロール状態同期を最大約30fpsへ集約
- 自動主目盛り・補助目盛り
- 世紀／年／月／日階層
- fit-range
- 左固定情報列
- 行仮想化
- 表示範囲クリッピング
- 密度切替
- 初期密度と反対方向へ切り替えた場合の仮想行再計測

## テスト

- 座標相互変換
- 月日境界
- うるう年
- ズーム中心維持
- 目盛り粒度選択
- グラデーション幅
- 継続中の当日終端
- 1,000行の仮想化
- E2E：パン・ズーム・fit-range
- パン中のグリフ表示維持
- 連続スクロール通知のフレーム単位集約
- 単一時点データの表示範囲補正
- 1024px以上での対象種別パネル幅
- E2E：設定・対象種別パネル、詳細情報、密度往復切替

## 完了条件

- 主要操作が滑らかに動作
- 全検証成功

---

# Phase 6: イベントアイテム・詳細表示

進捗：実装完了（Phase 7の開始待ち）

## 目的

イベントアイテム登録、タイムラインアイテム／イベントアイテムの詳細ページ、URL連動オーバーレイを実装する。初期版では単一親への紐づけを実装しつつ、将来の多対多関連と関連線描画へ拡張しやすいDB・API設計を残す。

## DB

- `timeline_events`
- 期間型親のみ許可
- 将来の多対多関連を見据えた `entity_relationships` の準備
- 所有者RLS
- 公開SELECT準備
- `timeline_items` と `timeline_events` の `summary` カラム削除

## UI

- メイン追加メニューから「タイムラインを追加」「イベントを追加」を選択
- 幅を広げた右サイドパネルから追加
- 詳細ページから追加
- 行ダブルクリックから日付プリセット
- ズーム連動スナップ
- 仮マーカー
- タイトルと日付または期間を固定ラベルなしで表示する共通ホバーUI
- タイムラインアイテム／イベントアイテムのホバー輪郭強調
- タイトルまたはタイムライングリフのクリック詳細
- ダブルクリック編集
- URL連動オーバーレイ
- 詳細オーバーレイの編集ボタンから同じオーバーレイ内の編集表示へ切替
- D&D領域の安定IDによるSSR hydration整合
- 年月日を含む日付並べ替えと、グループ化中の行内種別名省略
- 直接アクセス時の通常詳細ページ
- 大きなタイトル、プロパティ領域、本文領域を分けた共通詳細デザイン
- 本文・出典・外部URLを枠で囲わず編集できるフォームデザイン
- 目盛り単位を控えめな補助テキストとして表示
- 将来の関連線描画へ拡張しやすいデータ整形と表示構造の整理

Phase 6時点ではフォームと詳細表示の最大化を提供しない。詳細モーダルの全画面化はPhase 8.5へ追加し、フォーム自体の最大化は初期リリース後のレベルアップ要素として残す。

## テスト

- 時点型親への登録拒否
- 親期間外警告
- 日付スナップ
- イベントCRUD
- オーバーレイURL
- ブラウザ戻る
- 直接URL
- E2E：ダブルクリック登録
- E2E：追加メニュー、両詳細表示、ホバーUI
- E2E：リロード時のhydration警告なし、詳細からのモーダル編集切替、編集フォーム余白

## 完了条件

- 2つの登録経路が成立
- 全検証成功

---

# Phase 7: クラスタリング

進捗：実装完了

## 目的

重なるマーカーをクラスタ化し、選択・詳細導線を実装する。

## 実装

- 表示座標による衝突判定
- 判定直径10px・追加余白0pxによる近接範囲の調整
- 通常点12px・クラスタ点24pxの実描画径による再衝突判定
- 接触時クラスタ化
- 連鎖クラスタ
- 件数表示
- ホバー一覧
- クリック選択モーダル
- 個別詳細への遷移
- ズームで自動解除
- リサイズ・パネル開閉時再計算

## テスト

- 完全重複
- 境界接触
- collisionPadding
- 連鎖クラスタ
- ズーム解除
- 件数
- キーボード選択
- E2E：クラスタ→選択→詳細

## 完了条件

- 少しでも重なる場合にクラスタ化
- 全検証成功

---

# Phase 7.5: コンパクトレーン表示

進捗：実装完了

## 目的

固定行を使う従来の「行表示」に加え、時間的に重ならないタイムラインアイテムを同じ高さへ自動配置する「コンパクトレーン表示」を実装する。Phase 8へ進む前に、イベントを含む占有範囲、対象種別グループ、二方向パン、表示モードのURL状態を一体として完成させる。

## DB・API

- 新規テーブル、カラム、RLS、APIは追加しない
- 配置基準には既存の `project_settings.initial_zoom_preset` を使用する
- レーン割り当ては表示専用計算とし、DBへ保存しない

## UI

- 「行表示」／「コンパクトレーン表示」の切り替え
- 既定は行表示
- 表示モードを `layout=row|compact` としてURLクエリへ同期・復元
- 表示密度のユーザー向け名称を「標準」／「高密度」へ変更し、「コンパクト」との用語衝突を解消
- コンパクトレーン表示では左固定情報列と項目ごとの固定行を非表示
- 項目名を開始位置または時点位置の上へ表示
- 対象種別と期間は常時表示せず、既存の共通ホバーUIから確認
- タイムラインアイテム名、横棒、時点型マーカー、イベントマーカーの既存詳細・編集操作を維持
- タイムライン領域のドラッグ、通常スクロールバー、トラックパッドによる左右・上下パン
- コンパクトレーン表示中は並び順操作とD&Dを無効化し、行表示へ戻したときに以前の並び順を復元
- 画面下部の「表示中」は、仮想化やグループの展開状態ではなく、非表示に設定されていない項目数を表示

## 配置計算

- `initial_zoom_preset` から初期表示時に解決した縮尺を配置基準に使用
- `fit-range` はタイムラインアイテムと期間外を含むイベントのfit対象範囲、および初期表示時の利用可能幅から基準縮尺を解決
- 期間型バー、時点型マーカー、全イベントマーカー、項目名、前後16pxの余白を合成して占有範囲を算出
- `ongoing` はサーバー基準の当日、`unknown` はフェードアウト終端まで占有範囲へ含める
- 親期間外のイベントも占有範囲へ含め、イベントより後に開始する別項目との接触を防止
- クラスタリング前の全イベントマーカー領域を使用し、クラスタ生成・解除でレーンを変えない
- 開始日時、占有終了日時、安定IDの順に処理し、衝突しない最上段レーンへ割り当て
- 同一レーンの項目間と、イベントマーカーから次項目までに固定余白を確保
- ズーム、パン、クラスタリング、画面リサイズでは割り当てを維持
- タイムラインデータ、イベントデータ、表示対象フィルター、グループ化、初期ズーム設定の変更時だけ再計算
- 決定的な純粋関数として実装し、結果をメモ化

## グループ表示

- 対象種別によるグループ化
- 対象種別ごとに独立したレーン計算
- 既存の対象種別順、グループ見出し、展開・折りたたみ、折りたたみ状態を維持
- 「非表示にした項目」は最下部の独立グループとし、展開時はグループ内でレーン計算

## 性能・アクセシビリティ

- 1項目1行を前提とせず、レーンとグループを単位に縦方向を仮想化
- パン中の高コスト状態同期を既存と同様に集約
- 表示モード切り替え後も選択、フォーカス、詳細オーバーレイの状態を可能な範囲で維持
- 表示モード切り替え、グループ見出し、折りたたみ、項目操作をキーボードで利用可能にする
- 項目名とツールチップ情報へキーボードおよびフォーカスによる代替アクセスを提供

## テスト

- 単体：占有範囲、前後余白、タイトル幅、時点型、`ongoing`、`unknown`
- 単体：期間内イベント、期間外イベント、複数イベント、クラスタリング前イベント領域
- 単体：開始日時・占有終了日時・安定IDによる決定的なレーン割り当て
- 単体：境界接触、完全接続、余白不足、最上段レーンの再利用
- 単体：対象種別ごとの独立計算と折りたたみ対象の除外
- コンポーネント：表示モード切り替え、左固定列非表示、項目名表示、並び順・D&D無効化
- コンポーネント：ズーム、パン、クラスタリング、リサイズでレーン割り当てを維持
- コンポーネント：データ、フィルター、グループ、初期ズーム変更時の再計算
- E2E：URLからの表示モード復元と行表示への復帰
- E2E：左右・上下パン、グループの展開・折りたたみ、ホバー、詳細表示
- E2E：親期間外イベントを含む項目間余白と、ズーム後のレーン維持
- 性能：1,000項目・10,000イベントでの配置計算、仮想化、パン操作

## 文書

- `SPEC.md`、`TASKS.md`、`README.md` の表示モード、配置規則、表示密度用語、操作方法を同期

## 完了条件

- 行表示を壊さず、コンパクトレーン表示へ切り替えられる
- 項目、タイトル、期間外を含むイベント、固定余白が同一レーン上で衝突しない
- ズーム変更後もレーン割り当てが変わらない
- 対象種別グループ内で独立してレーンが計算され、展開・折りたたみできる
- 左右・上下へパンでき、1,000項目・10,000イベント規模で仮想化が機能する
- 全検証成功

---

# Phase 8: フィルター・全体検索

進捗：実装完了（Phase 8.5の開始待ち）

## 目的

タイムライン内フィルターとアプリ全体検索を実装する。

## DB

- PGroonga拡張
- `search_documents`
- インデックス
- 更新同期
- 権限制御
- `private` スキーマの直接実行不可な同期トリガー
- 所有者データと公開データだけを返すRLS付き検索RPC

## タイムライン内

- フリーワード
- 対象種別
- 年代
- イベントアイテム有無
- 曖昧状態
- 個別色
- 表示状態
- 非一致非表示／薄表示
- 一致マーカー強調
- URLクエリ同期
- 長文を一覧APIへ同梱せず、PGroongaから一致IDだけを取得

## 全体検索

- ヘッダー検索
- 候補
- デバウンス
- 検索結果ページ
- 種類別表示
- 抜粋
- ページネーション
- 詳細URL
- 候補は300msデバウンスし、検索結果は1ページ12件で種類フィルター対応

## テスト

- 日本語検索
- 非公開データ漏洩防止
- 公開データ
- URL状態復元
- イベントアイテム一致時の親表示
- E2E：検索・絞り込み・戻る
- 単体：URL状態、複合条件、イベント一致時の親保持、抜粋、入力検証
- 統合：日本語PGroonga、同期、所有者・他ユーザー・匿名RLS、直接書き込み拒否

## 完了条件

- 2種類の検索が独立して動作
- 全検証成功

---

# Phase 8.5: 登録UI・タイムライン画面・詳細導線の洗練

## 目的

Phase 9の公開閲覧画面を作る前に、登録サイドパネル、タイムラインの1画面レイアウト、詳細表示、検索からの復帰導線を共通設計として完成させる。

## DB・API

- タイムラインアイテムと0件以上のイベントアイテムを同時作成し、親と各イベントを個別検証する入力スキーマ
- RLSを適用したDB関数で親を先にINSERTし、各イベントの失敗を独立して捕捉
- イベントの入力不正、権限不正、期間型親の制約違反、DB制約違反があっても親と成功済みイベントを保持
- 作成済みイベントと、失敗したイベントのタイトル・安全な理由を区別する部分成功レスポンス
- 既存のタイムラインアイテム単体作成との後方互換

## 登録サイドパネル

- 時間形式を期間／時点のセグメント選択へ変更
- おおよそ、個別色、表示状態等を補足付きの設定行へ整理
- 年入力の幅を必要量へ抑え、日付グループをレスポンシブ配置
- 個別色のカラーピッカー、プレビュー、16進値入力、上書き解除
- 期間⇔時点切替時に開始日／時点日、精度、曖昧状態を引き継ぐ
- 同一編集セッション内で非表示になった終了状態・終了日を保持
- 新規タイムラインアイテムへイベントを0件以上追加できるUI
- `/items/[itemId]/edit` とイベント管理UIを共通化
- イベント追加の一部または全部が失敗した場合、親の成功を表示し、失敗したタイトルを列挙して通知
- 長いフォームでも保存操作と入力エラーを見失わない配置
- キーボード操作、フォーカス、狭い画面でのはみ出し防止

## タイムライン画面

- PCの通常状態でdocument全体の上下スクロールをなくす
- ヘッダー、プロジェクト情報、ツールバーを除く残り高さいっぱいにタイムラインを配置
- プロジェクト説明を既定1行表示にし、あふれる場合だけ展開／折りたたみを表示
- 説明折りたたみ時の余白と `min-height` を調整
- 説明展開時は全文閲覧を優先し、必要なスクロールを許可
- タイムライン内の縦・横スクロールバーを細く、角丸内へ収まるデザインに統一

## 詳細モーダル・個別ページ

- タイムラインアイテム／イベントアイテムのモーダル内スクロール領域とスクロールバーを統一
- 閉じる操作と競合しない全画面化アイコン
- 全画面化で同じ詳細URLを通常ページとして表示
- 両個別ページの最大幅、左右余白、操作配置を統一
- 「左右の余白を縮小」オプションを両詳細ページで共有
- オーバーレイと通常ページのアクセシビリティを維持

## 検索・ナビゲーション

- 検索候補／検索結果からの詳細リンクに安全なアプリ内 `returnTo` を付与
- 検索語、種類フィルター、ページ番号を保持して検索結果へ戻る
- 通常詳細ページに遷移元を反映したパンくずを表示
- URL直接入力または不正な `returnTo` では所属プロジェクトのタイムラインへフォールバック
- 検索復帰がある場合もタイムラインへの副導線を常設
- モーダル最大化時に導線や操作が重ならないレイアウト
- 外部URL、プロトコル相対URL、不正な相対パスを拒否

## テスト

- 単体：期間⇔時点の日付・精度・曖昧状態引き継ぎ、`returnTo` 検証、説明の展開判定
- React：設定行、カラーピッカー、レスポンシブ日付配置、イベント追加・削除、余白切替、全画面化
- 統合：親のみ作成、親子同時作成、複数イベント、一部失敗、全イベント失敗、時点型親でのイベント失敗、所有者・他ユーザー・匿名RLS
- E2E：新規親子同時登録、時間形式往復、カラーピッカー、PCでの1画面表示、説明展開、縦横スクロール
- E2E：アイテム／イベントモーダルの全画面化、余白切替、検索結果復帰、URL直アクセス時のタイムライン導線
- Chromium以外を含む対象ブラウザで、スクロールバー装飾が操作を妨げないことを確認

## 完了条件

- 登録、タイムライン、詳細、検索復帰の各導線が画面サイズとアクセス経路に応じて一貫して動作する
- イベント追加が失敗しても親と成功したイベントが保存され、失敗したタイトルが利用者へ通知される
- PCの説明折りたたみ状態で `/timeline` のdocument全体に上下スクロールがない
- 全検証成功

---

# Phase 9: 公開・共有

## 目的

公開閲覧ページ、共有URL、匿名SELECT、noindexを完成させる。

## DB

- visibility
- public_id
- published_at
- 匿名SELECT用RLS
- 再発行

## UI

- 公開確認
- 非公開確認
- URLコピー
- 別タブ表示
- URL再発行
- 公開閲覧専用レイアウト
- 編集UI除外

## テスト

- 非公開404
- 公開匿名閲覧
- 所有者編集
- 他ユーザー編集拒否
- 非公開化の即時反映
- URL再発行で旧URL無効
- noindexヘッダー
- E2E：公開→匿名閲覧→非公開

## 完了条件

- 公開範囲とRLSが一致
- 全検証成功

---

# Phase 10: 入出力・モバイル・性能・仕上げ

## 目的

JSON／CSV、スマートフォン閲覧、性能、アクセシビリティを完成させる。

## JSON

- エクスポート
- プレビュー
- 複製／上書き／中止
- スキーマバージョン

## CSV

- 3CSV＋READMEのZIP
- UTF-8 BOM
- ID空欄生成
- 親照合
- エラー・警告
- トランザクション取り込み

## モバイル

- タッチパン
- ピンチズーム
- フィルター
- 詳細閲覧
- 編集機能の無効化と案内

## 性能

- 1,000項目
- 10,000イベント
- 検索ページネーション
- 表示範囲クリッピング
- プロファイルと改善

## アクセシビリティ

- キーボード
- フォーカス
- モーダル
- 色以外の状態表現
- ラベル
- コントラスト

## テスト

- JSON往復
- CSV往復
- 不正ファイル
- 大規模データ
- モバイルビューポート
- 公開ページ
- 総合E2E

## 完了条件

- `SPEC.md` の初期版完了条件をすべて満たす
- 全検証成功
