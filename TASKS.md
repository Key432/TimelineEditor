# TASKS.md

## 1. 文書の目的

本書は、現在提供中の機能を基盤として、追加機能を依存関係、重要度、データ移行の安全性、Supabase無料枠の容量制約を考慮して段階的に実装するための唯一の計画書である。完了済み機能の実装経緯は記載しない。

各Phaseは垂直スライス方式で実装する。DB、RLS、API、UI、テスト、入出力互換性、関連文書を同一Phase内で完成させる。

現行仕様ではタイムラインアイテムは既に対象種別を持つため、本書における「種別追加」は、主としてイベントアイテムへのイベント種別追加を指す。

---

## 2. 共通実装規則

各Phase終了時に以下を実施する。

1. 対象機能の単体テスト、統合テスト、E2Eテストを追加する
2. 既存機能の回帰テストを実行する
3. `pnpm verify:commit` を成功させる
4. `SPEC.md`、`TASKS.md`、`README.md` を同期する
5. JSON・CSV形式へ影響する場合はスキーマバージョンを更新する
6. DBマイグレーションの適用・ロールバック手順を確認する
7. 大規模データで性能とDB容量の増加量を計測する
8. コミット後に停止し、次Phaseの開始指示を待つ

原則として、前Phaseが完了するまで次Phaseへ進まない。

---

## 3. 優先度

- **P0：基盤・中核機能** — 後続機能のデータ構造や安全性へ影響するため先に実装する
- **P1：主要なレベルアップ機能** — 実用性や差別化へ直接寄与する
- **P2：分析・利用範囲の拡張** — 中核機能の安定後に実装する
- **P3：別製品・別実行環境** — Web版と独立した開発系列として扱う

---

## 4. Supabase無料枠を前提とした容量方針

本計画ではSupabase無料枠の範囲内で継続運用できることを非機能要件とする。利用可能容量と使用量は管理者がSupabase Dashboardで確認し、本アプリには容量監視API、容量診断画面、警告閾値、使用量通知を実装しない。各Phaseでは、保存量が増える機能のデータ重複、保持期間、削除方針、インデックス増加を設計・レビュー・性能試験で確認する。

### 4.1 容量の基本原則

- Web版は長期・無制限の履歴保存を行わない
- 画像、添付ファイル、生成PDF、生成PNG、生成SVGをSupabaseへ恒久保存しない
- Markdown原文とレンダリング済みHTMLを二重保存しない
- 検索用テキスト、統計結果、ネットワーク配置など再生成可能な派生データを履歴対象にしない
- 一覧API用データと詳細本文を必要以上に重複保存しない
- インデックスは検索・制約に必要なものだけを追加し、各Phaseでサイズを確認する
- PGroongaインデックスを含む検索領域の増加量を実装時に計測する
- PDF・SVG・PNGは要求時に生成し、レスポンス返却後に破棄する
- 自動保存の未送信下書きはIndexedDBへ保存し、Supabaseへ履歴として蓄積しない
- 統計値は原則オンデマンド集計または短時間キャッシュとし、恒久保存しない
- Force-directed等のネットワーク自動配置結果は原則保存しない

### 4.2 履歴・スナップショットの制限

Web版の履歴は次の方針とする。

- 全行コピーではなくJSON Patch相当の差分を保存する
- 連続入力は一定時間単位でまとめ、文字入力ごとに履歴を作らない
- 既定保持期間を設ける。初期候補は90日
- 1エンティティあたりの既定保持数を設ける。初期候補は20版
- 1プロジェクトあたりの履歴容量上限を設ける。初期候補は25MB
- 制限超過時は古い自動履歴から削除し、手動保護した版を優先して残す
- 完全なプロジェクトスナップショットはDBへ多数保持せず、JSONエクスポートとして利用者へ保存させる
- Web版で保持する手動チェックポイントは少数に限定する
- 無制限履歴、長期スナップショット、画像込みバックアップはWindows版限定候補とする

### 4.3 公開版の制限

下書きと公開版を分離する場合も、公開のたびに全世代を複製しない。

- Web版では現行公開版を中心に保持する
- 公開用テーブルへは公開表示に必要な列だけを格納する
- 検索用派生データやレンダリング済みHTMLは複製しない
- 過去公開版は最大1世代など少数保持とし、保持数は設定可能にする
- 長期の公開履歴はJSONバックアップまたはWindows版で保持する
- 容量が厳しい場合、Web版では「現行公開版のみ」とし、過去公開版への復元はエクスポート経由とする

### 4.4 Web版から除外可能な機能

容量または実装複雑性が無料枠に適さない場合、以下はWindows版限定としてよい。

- 無制限の変更履歴
- 無制限の公開スナップショット
- 画像・添付ファイル管理
- 大量の固定ネットワークレイアウト保存
- 大規模な事前集計データキューブ
- PDF・画像のサーバー保存

---

# Phase L0：レベルアップ基盤・データ互換性

優先度：P0

状態：完了（DB・JSON・CSVの基準版v1、旧形式移行、共通参照、Feature Flag、保持上限、互換性テスト基盤）

## 目的

大規模な日付モデル変更、エンティティ参照、履歴、自動保存を、現行データとの互換性を保って安全に実装できる基盤を整える。

## 実装

- 現行DBスキーマとJSON・CSV形式を基準バージョンとして固定
- JSON・CSVへ明示的なスキーマバージョンを付与
- バージョン別インポートマイグレーション機構
- タイムラインアイテムとイベントアイテムの共通参照型
- 削除済み・権限外エンティティ参照を安全に扱う共通処理
- Feature Flag基盤
- 履歴、ゴミ箱、公開複製データの保持上限を各機能で強制できる共通方針
- 後続Phaseのマイグレーション、インポート、参照解決を検証するテスト基盤

## 完了条件

- 現行データを失わず後続マイグレーションを適用できる
- 旧スキーマのJSON・CSVを新スキーマへ安全に移行できる
- 全検証成功

---

# Phase L1：歴史日付モデルの拡張

優先度：P0

状態：完了（紀元前・年／年代／世紀精度、原表記、正規化範囲、連続時間軸、JSON／CSV v2移行）

## 目的

紀元前、年代、世紀を検索・並べ替え・描画可能な歴史日付として実装する。

## 対応

- 西暦年月日
- 紀元前年月日
- 年単位
- 年代単位
- 世紀単位
- 曖昧な年・年代・世紀
- 開始と終了で異なる精度
- 原資料上の日付表記
- 暦法情報を保持できる拡張余地

## 規則

- 利用者向け表示に西暦0年を使わない
- 紀元前1年と西暦1年を連続して計算する
- 19世紀は1801年から1900年
- 1860年代は1860年から1869年
- 紀元前5世紀は紀元前500年から紀元前401年
- 年代・世紀は内部的に最小境界と最大境界を持つ
- 日付精度と「おおよそ」を別属性として扱う

## UI・検索

- 西暦／紀元前切り替え
- 日付精度選択
- 年、年代、世紀に応じた入力UI
- 表記プレビュー
- 紀元前から西暦へまたがるパン、ズーム、fit-range
- 紀元前を含む時間軸ラベル
- 正規化範囲による検索、フィルター、並べ替え

## 入出力

- JSON・CSVへ時代、精度、年代、世紀、原表記を追加
- 現行の西暦データから自動移行

## 完了条件

- 紀元前から現代まで連続した時間軸として扱える
- 年、年代、世紀が検索・描画へ反映される
- 全検証成功

---

# Phase L2：閲覧操作・保存済みビュー

優先度：P1

## 目的

タイムラインの移動、年代把握、表示状態の再利用を改善する。

## 実装

- タイムライン表示領域のアプリ内最大化
- 対応ブラウザでのFullscreen API利用
- Escによる復帰
- マウスポインター位置の垂直年代線
- 垂直線と重なるイベント・クラスターの強調
- ホバーUIまたはフォーカスポップオーバーから詳細を開く操作
- 時間スライサー
- 全期間ミニマップ
- 年代ジャンプ
- 表示位置ブックマーク
- 保存済みビュー

保存済みビューには以下を含める。

- 表示年代
- ズーム・スクロール位置
- フィルター
- タグ条件
- 並び順・グループ
- 行表示／コンパクトレーン
- 背景レイヤー
- 関係線表示
- 表示列

保存済みビューは小さな設定JSONだけを保存し、データ本体を複製しない。

## 完了条件

- 特定年代へ素早く移動・抽出できる
- 複雑な表示状態を再利用できる
- 全検証成功

---

# Phase L3：詳細・編集UI統合と書体切り替え

優先度：P0

## 目的

詳細閲覧と編集を同一画面内で完結させる。

## 実装

- 詳細ページと編集ページを共通シェルへ統合
- 同一URLで閲覧・編集モードを切り替え
- URL連動オーバーレイ内でも同じ編集UIを利用
- 別編集ページへの移動を不要にする
- フォームの通常幅、ワイド、最大化
- タイムライン、検索結果、スクロール位置を維持
- タイムラインアイテムとイベントアイテムの共通フォーム部品化
- 未保存変更の破棄・明示保存
- タイムラインアイテム・イベントアイテムの通常詳細ページ、URL連動オーバーレイ、公開詳細ページでゴシック体／明朝体を切り替え
- 書体設定は両エンティティの詳細表示で共有し、ブラウザへローカル保存する。プロジェクトデータやSupabaseへは保存しない
- 日本語のシステムフォントスタックを使用し、外部Webフォントやフォントファイルを保存しない
- 書体切り替えをキーボード操作・読み上げ可能にし、選択状態を色だけに依存させない

## テスト

- 共通詳細シェルの閲覧／編集モード切り替え
- 通常詳細ページとURL連動オーバーレイのフォーム共通化
- ゴシック体／明朝体の切り替え、両エンティティ間の共有、再読み込み後の復元
- 公開詳細ページでの書体切り替えとキーボード操作

## 完了条件

- 詳細から画面遷移せず編集できる
- 通常ページとオーバーレイで編集構造が共通化される
- 全検証成功

---

# Phase L4：変更履歴・ゴミ箱・復元

優先度：P0

## 目的

完全自動保存、一括編集、内部リンク導入前に、誤操作からデータを復元できるようにする。

## 実装

- アイテム・イベントの差分履歴
- 変更前後の比較
- 特定版への復元
- ゴミ箱
- ゴミ箱からの復元
- 一括操作単位のUndo
- 削除前の依存関係・参照件数表示
- 手動チェックポイント
- 履歴保持期限・保持数・容量上限
- 古い自動履歴の自動整理
- JSONバックアップ作成の導線

## 容量最適化

- 全レコードの完全コピーは禁止
- 差分だけを保存
- 一定時間内の連続変更を1履歴へ統合
- 検索インデックス、統計、レンダリング結果は履歴対象外
- ゴミ箱内データも保持期限を設ける
- 容量超過時は古い自動履歴から削除する
- 無制限履歴はWindows版限定とする

## 完了条件

- 誤編集・誤削除を期限内に復元できる
- 履歴容量が設定上限を超えて増え続けない
- 全検証成功

---

# Phase L5：安全なMarkdown本文

優先度：P0

## 目的

タイムラインアイテムとイベントアイテムの本文を安全なMarkdownとして編集・表示する。

## 対応

- 見出し、太字、斜体、打ち消し線
- インラインコード、コードブロック
- 箇条書き、番号付きリスト
- 引用、水平線
- 表
- 外部リンク
- NOTE、TIP、IMPORTANT、WARNING、CAUTION等のコールアウト
- 入力中の即時プレビュー
- 分割表示と編集／プレビュー切り替え

## 非対応

- Markdown画像
- ファイル添付
- iframe、video、audio、SVG埋め込み
- raw HTML
- script、style
- JavaScript URL、data URL

## 安全性・容量

- Markdown原文だけを保存し、レンダリング済みHTMLは保存しない
- raw HTMLを解釈しない
- AST変換後に許可要素だけを描画
- 外部リンクのプロトコルを制限
- 検索インデックスには装飾を除いた必要最小限のテキストを使用

## 完了条件

- Markdownが入力中に反映される
- HTML直書きや外部埋め込みを実行しない
- 全検証成功

---

# Phase L6：プロジェクト内リンク・別名

優先度：P0

## 目的

Markdown本文から同一プロジェクト内のアイテムを安定IDで参照する。

## 記法

利用者入力は `[[項目名]]` とし、候補選択後に内部的には次の形式へ正規化する。

```md
[[item:UUID|表示名]]
[[event:UUID|表示名]]
```

## 規則

- `[[` 入力時にプロジェクト内候補を表示
- 同名候補には種類、日付、親を表示
- 名称だけでは自動確定しない
- リンク解決は名称ではなくIDで行う
- 名称変更後もリンクを維持
- 削除済みリンクは本文を残し、リンク切れとして表示
- 権限外と不存在を外部利用者から区別できないようにする
- 被参照アイテム削除時に参照元件数を表示
- プロジェクト複製・CSV取込時にIDを変換

## 別名

- 正式名称、原語名、旧名、筆名等を複数登録
- 検索、内部リンク候補、重複検出へ利用
- タイトル変更時に旧名称を別名へ追加できる

## 容量最適化

- 本文内のID参照を別の完全コピーとして保存しない
- 参照逆引きが必要な場合は小さな関連テーブルまたは更新時抽出を使用する
- 本文全文を参照索引へ重複保存しない

## 完了条件

- 名称変更後も内部リンクが維持される
- 同名、削除済み、権限外を安全に扱える
- 全検証成功

---

# Phase L7：構造化された出典・参考文献

優先度：P1

## 目的

共通の資料マスタを持ち、複数アイテムから同じ文献を参照できるようにする。

## 実装

- 資料名、著者、出版社、刊行年、ISBN、URL、参照日、引用キー、注記
- アイテム・イベントとの多対多関連
- ページ番号、章、引用箇所等の個別ロケータ
- 出典から参照中アイテム一覧
- 出典未設定項目の抽出
- Markdown本文中の引用記法
- BibTeX、CSL JSON等の入出力候補
- 将来のZotero連携余地

## 容量最適化

- 同じ書誌情報を各アイテム本文へ複製しない
- 書誌情報は資料マスタへ一度だけ保存する
- 外部ページ本文や書影は保存しない

## 完了条件

- 同一資料を複数項目から再利用できる
- 引用箇所を項目ごとに指定できる
- 全検証成功

---

# Phase L8：完全自動保存

優先度：P0

## 依存

- Phase L3
- Phase L4
- Phase L5
- Phase L6

## 実装

- 入力停止後のデバウンス保存
- フォーカス移動時の即時保存
- 保存中の追加変更を次キューへ送る
- 同一内容の重複送信防止
- 保存済み、保存中、未保存、失敗、オフライン、再試行中の表示
- 更新バージョンによる楽観的ロック
- 複数タブ競合の検出
- IndexedDBへの未送信下書き
- ブラウザ再起動後の復元
- DB保存成功後のローカル下書き削除

## 容量最適化

- 自動保存ごとに完全スナップショットを作らない
- 履歴はPhase L4の統合単位へまとめる
- 未送信下書きはSupabaseではなくIndexedDBへ保存
- 同一内容は更新・履歴作成しない

## 完了条件

- 通常編集で保存ボタンを不要にできる
- 通信失敗時にも入力内容を失わない
- 履歴が入力頻度に比例して無制限増加しない
- 全検証成功

---

# Phase L9：タグ・イベント種別・カスタムフィールド

優先度：P1

## タグ

- タイムラインアイテムとイベントアイテム共通のタグマスタ
- 多対多関連
- 名称変更、統合、未使用タグ削除
- タグによる検索・AND／ORフィルター
- 任意色と説明

## イベント種別

- 出版、受賞、移住、就任、展覧会、公演等
- イベント専用種別マスタを基本案とする
- マーカー形状・アイコンによる区別
- 色だけに依存しない

## カスタムフィールド

- プロジェクトまたは対象種別ごとに定義
- 文字列、複数行、数値、真偽値、単一選択、複数選択、URL、歴史日付、他アイテム参照
- テーブルビューで列表示
- 必須・任意設定
- 検索・統計へ利用可能な型情報

## 容量最適化

- タグ名を各アイテムへ配列複製せず関連テーブルで管理
- カスタムフィールド定義と値を分離
- 空値を保存しない
- JSONBを使う場合も、検索対象すべてへ無条件の汎用GINインデックスを作らない

## 完了条件

- 両エンティティへ自由タグを付与できる
- イベントを種別で分類できる
- 用途固有の属性を追加できる
- 全検証成功

---

# Phase L10：イベントの複数親対応

優先度：P0

## 目的

1つのイベントを複数の期間型タイムラインアイテムへ紐づけ、それぞれの行・レーンへ表示する。

## データモデル

- `timeline_event_item_links` 関連テーブル
- 主親 `is_primary`
- 親子所属と意味的関係を分離
- 現行 `timeline_item_id` からの安全な移行

## UI・表示

- 複数親検索・選択
- 親ごとの期間外警告
- 各親行への同一イベント表示
- 表示マーカー数とイベント実体数を区別
- 通常ページでは主親をパンくずの既定値にする
- 遷移元親が明確な場合はその親を表示
- クラスタリングとコンパクトレーンへ対応

## 完了条件

- 1イベントを複数親上へ表示できる
- 検索、統計、CSVでイベントを二重計上しない
- 全検証成功

---

# Phase L11：テーブルビュー・一括操作・汎用CSV取込

優先度：P1

## テーブルビュー

- タイムラインアイテム表・イベント表
- 列表示切り替え、列幅、固定列
- 仮想スクロール
- セル直接編集
- 行追加、複数行選択
- キーボード移動、コピー・貼り付け
- タグ、種別、親、カスタムフィールド用セル

## 一括操作

- タグ追加・削除・置換
- 対象種別・イベント種別変更
- 表示状態、個別色変更
- 複数アイテム・イベント削除
- 依存関係と参照件数の事前表示
- 一括操作単位のUndo

## CSV

- 選択データだけのCSVエクスポート
- 関連する種別、タグ、親、関係を含める選択肢
- 任意CSVの列マッピング
- 固定値設定
- 日付形式指定
- マッピング設定保存
- 重複候補表示
- エラー行のみ再出力

## 容量最適化

- 一括変更時に行ごとの履歴を無制限作成せず操作単位で圧縮
- インポートファイルをSupabaseへ恒久保存しない
- プレビュー用データは一時処理とする

## 完了条件

- 表から大量データを編集できる
- 安全な一括変更と外部CSV取込ができる
- 全検証成功

---

# Phase L12：年代背景レイヤー

優先度：P1

## 実装

- 時代区分、王朝、政権、文化潮流等の複数レイヤー
- レイヤー単位の表示・非表示
- レイヤー並べ替え
- 背景期間の色、説明、開始・終了
- 紀元前、年代、世紀対応
- 曖昧開始・終了のグラデーション
- 同一レイヤー内の重複警告
- 行表示・コンパクトレーン対応
- 公開ページ対応
- 保存済みビューとの連携

## 容量最適化

- 背景期間は通常アイテムへ複製しない
- 描画結果を保存しない
- 背景レイヤー用の検索インデックスは必要になるまで追加しない

## 完了条件

- 複数の時代区分を背景へ重ねて表示できる
- 全検証成功

---

# Phase L13：データ品質チェック・重複統合

優先度：P1

## 品質チェック

- 内部リンク切れ
- 削除済み参照
- 親を持たないイベント
- 全親の期間外にあるイベント
- 出典、本文、必須カスタムフィールドの不足
- 不正な外部URL
- 未使用タグ・種別
- 孤立した関係
- Markdown記法上の問題
- 公開中だが未完成の項目

## 重複検出

- 正規化タイトル
- 別名
- 日付
- 種別
- 親
- 外部URL
- 類似文字列

## 統合

- イベント、タグ、内部リンク、関係、出典、カスタムフィールド、複数親を統合
- 削除側IDへの参照を存続側IDへ変換
- 統合前のプレビューとUndo

## 容量最適化

- 類似度計算結果を恒久保存しない
- 診断結果はオンデマンド生成または短時間キャッシュ
- 大量の重複候補を履歴として保存しない

## 完了条件

- データ不整合を一覧から修正できる
- 重複アイテムを参照関係ごと統合できる
- 全検証成功

---

# Phase L14：意味的関係とタイムライン上の矢印

優先度：P1

## 実装

- influence、reference、collaboration、teacher、opposition、succession、other等
- 方向あり・方向なし
- タイムラインアイテム／イベントの全組み合わせ
- 関係種別、方向、注記
- 詳細ページでの追加・編集・削除
- 選択中アイテムの直接関係だけを表示する既定モード
- 表示範囲内の全関係表示オプション
- 線・矢印のホバー、クリック
- コンパクトレーン・クラスター対応

## 表示方針

- 全関係線の常時表示を既定にしない
- 通常時は非表示または選択対象の直接関係だけ表示
- 大量線表示はネットワークビューへ分離する

## 完了条件

- アイテム間の意味的関係を登録・表示できる
- 大量の線で通常タイムラインを破壊しない
- 全検証成功

---

# Phase L15：関連ネットワークビュー

優先度：P2

## 実装

- アイテムとイベントをノード表示
- 関係を方向付き／方向なしエッジ表示
- タグ、種別、年代、関係種別フィルター
- 時間スライサー連携
- ノード検索
- 直接関係・2段階関係の強調
- ノードから詳細を開く
- Force-directedを初期レイアウトとする
- 大規模データの段階展開・クラスタ化

## 容量最適化

- 自動計算したノード位置を原則DBへ保存しない
- 利用者が明示固定した位置だけ保存する余地を残す
- 事前計算済みグラフを大量保存しない

## 完了条件

- 関係データを独立したネットワークとして探索できる
- 全検証成功

---

# Phase L16：下書きと公開版の分離

優先度：P1

## 目的

編集中の状態を公開ページへ即時反映せず、整合性のある版だけを公開する。

## 実装

- 下書き
- 公開済み
- 未公開変更あり
- 公開前プレビュー
- 変更を公開
- 現行公開版へ戻す
- 公開日時と公開リビジョン

## 容量最適化

- 公開のたびに全世代を複製しない
- 公開表示に必要な列だけを公開用領域へ保持
- Web版では現行公開版を基本とする
- 過去公開版は最大1世代など少数に限定
- 過去版の長期保管はJSONエクスポートまたはWindows版へ委ねる
- 容量不足時は過去公開版保持を無効化できる

## 完了条件

- 編集途中の状態を公開せずに済む
- 公開用データが世代数に比例して無制限増加しない
- 全検証成功

---

# Phase L17：SVG・PNG・PDF出力

優先度：P1

## 方針

共通の出力描画モデルを作り、SVGを基本としてPDF・PNGへ展開する。

## 実装

- 現在の表示範囲
- 時間スライサー範囲
- 全項目範囲
- 任意年代範囲
- 行表示／コンパクトレーン
- フィルター、グループ、背景レイヤー、関係線
- タイトル、説明、凡例
- 用紙サイズ、向き、余白
- 1ページ縮小／複数ページ分割
- 日本語フォントとページ境界確認

## 容量最適化

- 生成ファイルをSupabase StorageやDBへ保存しない
- サーバー生成時も一時ファイルとして処理後に削除
- 可能な場合はブラウザ側生成を優先
- 出力履歴はファイル本体ではなく任意の小さな設定だけを保存

## 完了条件

- 編集可能なSVGと印刷可能なPDFを出力できる
- Supabase容量を恒久消費しない
- 全検証成功

---

# Phase L18：プロジェクト横断比較

優先度：P2

## 実装

- 複数プロジェクトを共通時間軸で表示
- 所有プロジェクトと閲覧可能な公開プロジェクト
- 共通パン、ズーム、時間スライサー
- プロジェクト単位の表示・非表示
- タグ、種別、イベント種別フィルター
- 詳細から比較状態へ復帰
- 比較画面は閲覧専用とし、編集機能を提供しない
- 比較条件は保存済みビューとして保存可能

## 容量最適化

- 比較用データを複製保存しない
- プロジェクトごとに段階取得
- 表示年代付近だけを取得するAPIを検討
- 比較ビューは設定JSONだけを保存

## 完了条件

- 複数年表を同じ年代で比較できる
- 全検証成功

---

# Phase L19：統計情報

優先度：P2

## 集計候補

- アイテム・イベント件数
- 対象種別・イベント種別・タグ別件数
- 年代・世紀別件数
- 存続期間分布
- 曖昧日付・終了状態別件数
- イベントを持たないアイテム
- 複数親イベント
- 関係種別と関係数
- 内部リンク数・リンク切れ
- 本文・出典未入力件数

## 表示

- KPI、棒、折れ線、ヒストグラム、ヒートマップ、集計表
- 時間スライサー・フィルター連動
- 集計結果から該当項目へ移動
- CSV出力

## 容量最適化

- 統計値は原則オンデマンド計算
- 長期の集計スナップショットを保存しない
- 必要な場合のみ短時間キャッシュ
- 大規模な分析履歴やデータキューブはWindows版限定候補

## 完了条件

- プロジェクトの構成と不足を把握できる
- 全検証成功

---

# Phase L20：ログイン前ローカルモード・PWA

優先度：P2

## 目的

未ログインでもブラウザ内で年表を作成し、Supabase容量を消費せず利用できるようにする。

## 実装

- IndexedDB保存
- ローカル専用ID
- IndexedDBスキーママイグレーション
- 完全自動保存
- ローカル全文検索
- JSON・CSV入出力
- ブラウザ容量不足警告
- PWAインストール
- オフライン閲覧・編集
- ログイン後のプロジェクト選択取込
- ID、内部リンク、複数親、関係の一括変換
- 取込成功までローカルデータを削除しない

## 容量方針

- ローカルプロジェクトはSupabaseへ自動同期しない
- 利用者が明示的に取り込んだ場合だけクラウド容量を消費する
- 大容量プロジェクトは取込前に概算容量を表示する

## 完了条件

- 未ログインで年表を作成・保存できる
- ログイン後に安全にクラウドへ移行できる
- 全検証成功

---

# Windowsネイティブアプリ系列

優先度：P3

Windows版はWeb版の一機能ではなく独立製品として扱う。Web版のスキーマ、歴史日付、Markdown、内部リンク、タグ、複数親、関係、CSV形式が安定してから開始する。

## Phase W1：設計

- 技術選定
- ローカルDB
- 自動更新
- インストーラー
- データ保存場所
- バックアップ
- Web版との共有パッケージ範囲

## Phase W2：基本版

- ログインなし
- ローカルプロジェクト
- 主要タイムライン機能
- Markdown、内部リンク、タグ、カスタムフィールド
- 複数親、関係、テーブルビュー
- JSON・CSV
- 自動保存

## Phase W3：画像・長期保存

Windows版限定候補：

- 詳細ページへの複数画像
- アプリ管理フォルダへの画像コピー
- サムネイル
- 画像込みZIPバックアップ
- 無制限または大容量の変更履歴
- 長期スナップショット
- 長期の公開相当スナップショット
- 大規模統計キャッシュ

Web互換CSVには画像バイナリを含めず、画像は別マニフェストで管理する。

## Phase W4：互換性検証

- Web版CSVをWindows版へ取込
- Windows版CSVをWeb版へ取込
- 紀元前、世紀、年代
- Markdown、内部リンク、タグ
- カスタムフィールド、複数親、関係、背景レイヤー
- 未対応列の安全な無視
- スキーマバージョン差異警告

---

# 実装順序一覧

|   順序 | Phase | 機能                                           | 優先度 |
| -----: | ----- | ---------------------------------------------- | ------ |
|      1 | L0    | 基盤・データ互換性                             | P0     |
|      2 | L1    | 紀元前・世紀・年代表現                         | P0     |
|      3 | L2    | 最大化・年代線・時間スライサー・保存済みビュー | P1     |
|      4 | L3    | 詳細・編集UI統合と書体切り替え                 | P0     |
|      5 | L4    | 変更履歴・ゴミ箱・復元                         | P0     |
|      6 | L5    | 安全なMarkdown                                 | P0     |
|      7 | L6    | 内部リンク・別名                               | P0     |
|      8 | L7    | 構造化出典                                     | P1     |
|      9 | L8    | 完全自動保存                                   | P0     |
|     10 | L9    | タグ・イベント種別・カスタムフィールド         | P1     |
|     11 | L10   | イベント複数親                                 | P0     |
|     12 | L11   | テーブル・一括操作・汎用CSV                    | P1     |
|     13 | L12   | 年代背景レイヤー                               | P1     |
|     14 | L13   | データ品質・重複統合                           | P1     |
|     15 | L14   | 関係登録・矢印                                 | P1     |
|     16 | L15   | 関連ネットワーク                               | P2     |
|     17 | L16   | 下書きと公開版の分離                           | P1     |
|     18 | L17   | SVG・PNG・PDF出力                              | P1     |
|     19 | L18   | プロジェクト横断比較                           | P2     |
|     20 | L19   | 統計情報                                       | P2     |
|     21 | L20   | ローカルモード・PWA                            | P2     |
| 別系列 | W1–W4 | Windowsネイティブアプリ                        | P3     |

---

# 主要依存関係

```text
歴史日付モデル
├─ 時間スライサー・年代ジャンプ
├─ 年代背景レイヤー
├─ 出力
├─ プロジェクト横断比較
└─ 統計情報

詳細・編集UI統合
├─ 変更履歴・ゴミ箱
├─ Markdown
│  └─ プロジェクト内リンク
└─ 完全自動保存

タグ・イベント種別・カスタムフィールド
├─ テーブルビュー
├─ 一括編集
├─ データ品質チェック
└─ 統計情報

イベント複数親
├─ テーブルビュー
├─ 関係表示
├─ 横断比較
└─ 統計情報

関係登録
└─ ネットワークビュー

安定したデータ形式
├─ ローカルモード・PWA
└─ Windowsネイティブアプリ
```

---

# 初期対象外

- Web版への画像・ファイル添付
- Web版Markdownへの画像埋め込み
- iframeや外部ページ埋め込み
- raw HTML
- リアルタイム共同編集
- Web版とWindows版の自動同期
- Windows版のログイン
- Windows版からの公開URL発行
- AIによる自動関係抽出・自動本文生成
- 複数利用者間のコメント・レビュー
- Web版での無制限履歴・無制限スナップショット
- Web版での生成PDF・画像の恒久保存
