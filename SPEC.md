# タイムライン作成支援Webアプリ 仕様書

このファイルは仕様書の索引である。仕様本文は機能領域ごとに `docs/spec/` へ分割しており、実装時は対象Phaseに関係する文書だけを追加で読む。実装済みかどうかと実装順序は `TASKS.md` を正とする。

ユーザー向けUIでは、タイムラインアイテムを分類する従来の「対象種別」を「タイムライン種別」と表記する。DB・APIの `item_type` / `type_id` は互換性維持のため変更しない。

イベントの複数の親タイムラインアイテムはすべて同格とし、主親・副親を設けない。表示順とパンくず内の選択肢は関連の手動 `sort_order` に従う。

単体イベントマーカーは種別なしと「丸」を同じ円形とし、他形状を含めて白い円形縁取り込みでPhase L9以前と同じ12px径にする。タイムラインから追加するときはスナップした日付精度と年月日の入力欄を一致させる。

分類の色・アイコン・形状変更はタイムラインへページ再読込なしで反映する。資料管理はタイムラインの右サイドパネルとプロジェクトタイルから利用でき、共通UIを「資料マスタ」「新しい資料」「出典未設定の項目」の順で表示する。

表示モードは行表示、コンパクト、テーブルを切り替えられる。テーブルはタイムライン表／イベント表、固定・表示・ドラッグ並べ替え・複数行折り返し対応列、セル直接編集、一括操作Undo、選択CSVと任意CSVマッピングを提供する。列の表示・非表示と順番は単一の列管理一覧で操作する。タイムライン表には行表示と同じ並び順、フィルター、種別グループ、折りたたみ状態を適用する。

スマートフォンの行表示では固定情報列を項目名中心に縮小し、行操作をメニューへ集約して、時間軸の可視領域を確保する。閲覧専用画面では不要な並べ替え・編集列の空白を表示しない。

年代背景は通常アイテムと分離した複数レイヤーとして保持し、行表示・コンパクト・公開ページへ同じ時間座標で描画する。レイヤー表示は保存済みビューへIDだけを保存し、背景データや描画結果は複製しない。

意味的関係はタイムラインアイテム／イベントの全組み合わせを扱い、日本語の既定種別と自由入力、単線／二重線、始点／終点矢印を保持する。タイムラインでは半透明灰色の「標準」、着色する「すべて表示」、「すべて非表示」を切り替え、垂直・水平・垂直の直角線を項目より前面、固定列・編集列・グループ見出しより背面へ描画する。両端が表示範囲外でも経路が表示年代を横切る関係線は描画対象に含める。仮想行の重なりコンテキストを越えて固定列を前面化できないため、固定列との境界はReactの間引き済みスクロール状態ではなく、DOMの実スクロール位置から同一イベント内でSVG描画領域へ反映する。詳細表示はリンク付きの閲覧専用一覧とし、編集モードと新規追加パネルから関係を変更する。管理UI名は「分類・関係」とする。

関連ネットワークではタイムラインを直角四角、イベントを角丸四角で区別し、種別を示す文字は表示しない。通常ノードはタイトル幅に応じて横172〜250px、高さ76pxとし、タイトルは最大2行、分類名は1行で、収まらない末尾を「…」で省略する。分類名の長さはノード幅へ影響させない。

タイムラインの常設ツールバーは、頻用する配置設定とフィルターを直接表示し、年代背景・関係線を「表示」、最大化・全画面を「画面」、年代移動・位置ブックマーク・保存済みビューを「移動・ビュー」へ分類する。

## 仕様書一覧

- [プロダクト・技術・権限・API（1〜7章）](docs/spec/01-product-and-architecture.md)
- [データモデル・プロジェクト・エンティティ（8〜11章）](docs/spec/02-data-and-projects.md)
- [タイムライン・編集・詳細（12〜17章）](docs/spec/03-timeline-and-editing.md)
- [検索・公開・入出力（18〜21章）](docs/spec/04-search-sharing-import.md)
- [レイアウト・端末・品質・性能（22〜27章）](docs/spec/05-layout-quality-performance.md)
- [レベルアップ機能・完了条件（28〜39章）](docs/spec/06-level-up-features.md)

## 実装時の参照

1. `TASKS.md` で対象Phaseを確認する
2. `docs/phases/<phase>.md` を読む
3. 上記一覧から関連仕様だけを読む
4. 強制実装規則は `AGENTS.md` に従う
