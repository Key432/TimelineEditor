name: Apply future architecture specification

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-future-architecture-spec.yml

permissions:
  contents: write

jobs:
  update-docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Update SPEC.md and TASKS.md
        shell: bash
        run: |
          python <<'PY'
          from pathlib import