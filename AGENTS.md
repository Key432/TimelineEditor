# AGENTS.md

## 1. このリポジトリでの役割

このファイルは、Codexが本リポジトリで実装を行う際の強制ルールである。

作業開始前に必ず以下を読むこと。

1. `SPEC.md`
2. `TASKS.md`
3. 本ファイル
4. 作業対象ディレクトリ配下に追加の `AGENTS.md` がある場合はそれも読むこと

仕様と実装が食い違う場合、勝手に補完しない。重大な不明点として報告し、ユーザーの判断を待つこと。

---

## 2. フェーズ制

- 実装は `TASKS.md` のフェーズ順で進める。
- ユーザーから明示された1フェーズだけを実装する。
- 対象フェーズの完了後は停止する。
- ユーザーの続行指示なしに次フェーズへ進まない。
- 将来フェーズの先行実装は、現フェーズに不可欠な最小限の基盤を除いて禁止する。
- 先行基盤を追加した場合は、理由を完了報告へ記載する。

---

## 3. 垂直スライス

各フェーズでは、必要な層を一式で完成させる。

- DBマイグレーション
- RLS
- repository/service
- API
- UI
- 入力検証
- 単体テスト
- 統合テスト
- E2Eテスト
- ドキュメント

UIだけ、APIだけ、DBだけを未接続のまま完成扱いにしない。

---

## 4. テスト必須規則

### 4.1 新規機能

新規機能を追加する場合、その機能を網羅するテストを同じ変更内へ必ず追加する。

対象に応じて以下を使い分ける。

- 純粋ロジック：Vitest
- Reactコンポーネント：React Testing Library
- API・DB・RLS：統合テスト
- 主要ユーザーフロー：Playwright

最低限、次を検討する。

- 正常系
- 境界値
- 入力不正
- 権限不正
- 非公開データへのアクセス
- 存在しないID
- 日付の前後関係
- 空値
- 重複
- UIのキーボード操作

### 4.2 不具合修正

原則として以下の順序を守る。

1. 不具合を再現するテストを追加する
2. 修正前にそのテストが失敗することを確認する
3. 実装修正
4. 対象テスト成功を確認する
5. 全検証を実行する

### 4.3 禁止事項

以下を禁止する。

- `git commit --no-verify`
- `.skip`、`.only`の残置
- 失敗テストの削除による回避
- 実装不具合に合わせた期待値変更
- 空のテスト、常に成功するだけのテスト
- フレーク原因を調査せず、偶然成功するまで再実行すること
- `any`、`@ts-ignore`、`@ts-expect-error`による安易な隠蔽
- 型検査・Lint・E2Eを一時的に外してコミットすること
- テスト対象を縮小して完成扱いにすること

---

## 5. コミット条件

### 5.1 Gitブランチ戦略

- 個人開発では基本的に `main` 以外のブランチを切らない。
- 目的ごとに `main` 上で作業し、実施内容を都度コミットする。
- 1つの作業単位は1つの論理変更または1つの目的にまとめ、履歴を追いやすくする。
- 例外的に一時作業や長期並列作業が必要な場合のみ、別ブランチを検討する。

### 5.2 コミットメッセージ

- コミットメッセージは英語で記述する。
- `fix: `、`feat: `、`docs: `、`refactor: `、`chore: ` などの接頭辞を付ける。
- 例: `fix: handle invalid project name`、`docs: add branching strategy guidance`

コミット前に必ず以下を実行する。

```bash
pnpm verify:commit
```

`verify:commit`は少なくとも次をすべて含むこと。

```bash
pnpm format:check
pnpm lint
pnpm typecheck
pnpm test:unit
pnpm test:integration
pnpm test:e2e
pnpm test:migrations
pnpm build
```

失敗した場合：

1. 原因を調査
2. 実装またはテストを修正
3. 対象検証を実行
4. 最後に `pnpm verify:commit` を最初から再実行

すべて成功するまでコミットしてはならない。

Git hookにはHusky等を用いてよい。pre-commitまたはpre-pushで `pnpm verify:commit` を実行する。ローカルフックだけでなくGitHub Actionsでも同じ検証を行う。

---

## 6. コミット単位

- 1フェーズにつき原則1つの完了コミットとする。
- フェーズ内で安全な中間コミットが必要な場合も、ユーザーへ報告する最終コミットは全検証成功後とする。
- 未完成・失敗状態を完成コミットとして残さない。
- フェーズ終了時にコミットし、そこで停止する。

---

## 7. フェーズ終了報告

完了後は以下を報告する。

```text
## 完了したフェーズ
フェーズ名

## 実装内容
- ...

## DB変更
- マイグレーション名
- RLS変更
- 制約変更

## テスト
- 追加した単体テスト
- 追加した統合テスト
- 追加したE2E

## 検証結果
- format
- lint
- typecheck
- unit
- integration
- e2e
- migrations
- build

## コミット
- コミットハッシュ

## 既知の制限
- ...

## 次フェーズ
- ...
```

テストを通せない場合はコミットせず、失敗内容、調査内容、試行した修正、残る障害を報告して停止する。

---

## 8. アーキテクチャ規則

### 8.1 データアクセス

- UIからSupabaseへ直接書き込まない。
- Route HandlerまたはServer Actionの採用可否は、対象機能の仕様に合わせる。
- 認証・所有権・公開状態の判定はサーバー側で行う。
- Supabaseクエリはrepository/serviceへ集約する。
- Route HandlerとServer Componentで同じクエリを重複実装しない。

### 8.2 状態管理

- 永続データ：Supabase
- APIキャッシュ：TanStack Query
- URL共有すべき表示条件：URLクエリ
- 一時的タイムライン状態：Zustand
- フォーム：React Hook Form
- バリデーション：Zod

同じ状態を複数箇所へ重複保存しない。

### 8.3 歴史日付

- 歴史日付へJavaScript `Date`を安易に使用しない。
- 年・月・日を分離した独自型を用いる。
- タイムゾーン変換による日付ずれを発生させない。
- 利用者向けに西暦0年を扱わず、紀元前1年と西暦1年を連続して計算する。
- 西暦・紀元前、年・年代・世紀、精度、おおよそ、原資料上の表記を混同せず独立した属性として扱う。
- 月未入力時に日を許可しない。
- 座標変換と逆変換は純粋関数として実装し、単体テストを充実させる。

### 8.4 タイムライン

- 時間軸、ズーム、クラスタリングは自前実装する。
- 巨大な単一SVGを作らない。
- 1,000項目・10,000イベントを性能目標とする。
- 縦方向を仮想化する。
- 高頻度状態変更で画面全体を再描画しない。
- パン中は高コスト処理を抑制する。
- クラスタリングは表示座標に基づいて判定する。

---

## 9. Supabase規則

- DB変更は必ず `supabase/migrations/` にSQLとして追加する。
- Supabase Dashboardだけでスキーマを変更しない。
- 主要テーブルではRLSを必ず有効化する。
- RLSポリシーを追加・変更した場合、所有者・他ユーザー・匿名の統合テストを追加する。
- 外部キーと削除動作を明示する。
- プロジェクト配下の削除は必要箇所で `ON DELETE CASCADE` を使う。
- 対象種別削除でタイムライン項目を連鎖削除してはならない。
- Service Role Keyをクライアントコード、ログ、テスト出力、コミットへ含めない。
- `.env*`の秘密値をコミットしない。
- PGroongaの有効化と検索インデックスもマイグレーションで管理する。

### 9.1 Supabase無料枠

- Web版はSupabase無料枠の範囲で継続運用できる保存設計とする。
- 本アプリ内に容量監視API、容量診断UI、警告閾値、使用量通知を実装しない。管理者はSupabase Dashboardで使用量を確認する。
- 履歴、ゴミ箱、公開版、検索索引、統計、出力等を実装するPhaseでは、重複保存、保持期間、削除方針、インデックス増加を確認する。
- 画像・添付ファイル・生成PDF／PNG／SVG・再生成可能な派生データをSupabaseへ恒久保存しない。
- 容量を増やす機能を追加した場合は、大規模データ試験で増加量を計測し、完了報告へ記載する。

---

## 10. セキュリティ規則

- クライアントから渡された `ownerId` を信用しない。
- 認証ユーザーIDはサーバーセッションから取得する。
- 非公開リソースは所有者以外へ返さない。
- 公開データの書き込みは所有者だけに限定する。
- 入力はZodで検証する。
- プレーンテキストはHTMLとして解釈せず、安全なMarkdownは許可したAST要素だけを描画する。
- URLを安全に検証する。
- Service Role使用時は、使用理由をコードコメントと完了報告に記載する。
- `noindex`は認可の代替にしない。

---

## 11. UI規則

- Web版はライトモードのみ。
- Primary：`#00B0B0`
- Secondary：`#FF3399`
- 本文：`#333333`
- 色だけで状態を伝えない。
- キーボード操作を実装する。
- フォーカスリングを消さない。
- モーダルはEsc、閉じるボタン、適切なフォーカストラップに対応する。
- 破壊的操作には確認を要求する。
- スマートフォンでも編集機能を提供し、タイムラインフィールドのクリック／タップによるイベント作成だけを無効にする。

---

## 12. 文書更新

仕様変更または実装上の重要判断を行った場合、同じ変更内で以下を更新する。

- `SPEC.md`
- `TASKS.md`
- `README.md`
- 必要なコードコメント
- API仕様またはDB説明

実装が文書を追い越した状態を残さない。

---

## 13. Codexが勝手に決めてよい範囲

以下は仕様を変えない範囲で選択可能。

- ファイル分割
- コンポーネント名
- 内部関数名
- テストデータの具体値
- アクセシビリティを改善する軽微なUI調整
- 性能改善のための内部実装
- 採用ライブラリの同等代替。ただし主要スタック変更はユーザー確認が必要

以下は確認なしに変更禁止。

- URL
- DBの意味
- 認証方式
- 公開範囲
- 削除方式
- 時間形式
- 曖昧日付の意味
- テスト・コミット規則
- フェーズ順
- デザインの主要色
